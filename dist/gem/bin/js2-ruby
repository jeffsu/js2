#!/usr/bin/env ruby

dir = File.expand_path('../../lib/', __FILE__)
dir = File.directory?(dir) ? (dir + '/') : ''

require "#{dir}js2"
require "#{dir}js2/command"
require "#{dir}js2/fs"

JS2::Command.new(ARGV)
