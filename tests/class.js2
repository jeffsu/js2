class Test1.Foo {
  var member      = "member";
  var regexMember = /member/;
  var stuffs      = [ 'hello', 'world' ];

  function getStuffs () {
    return this.stuffs;
  }
}

JS2.test(#(assert){
  var test = new Test1.Foo(); 
  assert.eq('hello', test.getStuffs()[0]);
});
