class Test1.Foo {
  var member      = "member";
  var regexMember = /member/;
  var stuffs      = [ 'hello', 'world' ];

  function getStuffs () {
    return this.stuffs;
  }
}

var test = new Test1.Foo(); 
console.log(test.getStuffs().join(' '));
JS2.assertEquals(test.getStuffs()[0], 'hello');
